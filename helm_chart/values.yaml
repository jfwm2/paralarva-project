# !!! this file was generated by build_and_deploy_chart.sh !!!
# !!!! do not modify manually as changes may be overridden !!!!

replicaCount: 1

image:
  repository: jfwm/paralarva
  pullPolicy: IfNotPresent
  tag: "0.2.0"

imagePullSecrets: []
nameOverride: "paralarva"
fullnameOverride: "paralarva"

serviceAccount:
  create: true
  annotations: {}
  name: paralarva-user

service:
  type: NodePort
  nodePort: 30303

ingress:
  enabled: false

autoscaling:
  enabled: false

app:
  command:
    interpreter: "/usr/local/bin/python"
    launcher: "main.py"
    confDir: "/opt/paralarva/config"
    confFileName: "config.yaml"
    logLevel: "info"
    timeout: 30
    dryRun: false
  data:
    service:
      type: NodePort
      port: 8000
      nodePort: 30300
  config:
    proxy:
      listen:
        address: "127.0.0.1"
        port: 8080
      services:
        - name: my-service
          domain: my-service.my-company.com
          hosts:
            - address: "10.0.0.1"
              port: 9090
            - address: "10.0.0.2"
              port: 9090

